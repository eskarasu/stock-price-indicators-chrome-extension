# stock-price-indicators
A Chrome extension and Flask server to display real-time stock prices. The extension allows users to input a stock symbol and fetches the latest price using the Flask server, which utilizes the Yahoo Finance API.
